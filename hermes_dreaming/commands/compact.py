from __future__ import annotations

# Phase 1 stub — full implementation in Phase 2+.
# Runs a focused Deep-only cycle: merge duplicates and remove obsolete entries.
# Does not add new entries.


def handle(args: str = "") -> str:
    return (
        "## /dreaming compact\n\n"
        "Not yet implemented (Phase 2+).\n\n"
        "Once implemented, this command will inspect MEMORY.md and USER.md for duplicates, "
        "contradictions, and clearly stale entries, then propose or apply compacting operations "
        "without adding new entries."
    )
